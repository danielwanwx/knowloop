---
name: coach
description: Short entry for the interview-coach learning loop and private progress dashboard. Use for $coach or an explicit request to 开练. Resume the same private learning records and progressive coaching workflow.
---

# Coach

This is an alias, not a separate coach. Load the installed `interview-coach/SKILL.md` and follow it. First check the sibling `../interview-coach/SKILL.md`; otherwise use the host skill catalog to locate the canonical skill. Resolve its scripts and references relative to its own directory.

An explicit `$coach` or “开练” is accepted coaching intent: begin directly, preserving the requested topic/mode or resuming prior context. Use the same configured private root and evidence store as interview-coach; never create a shortcut-specific record store. Do not require the learner to type the longer name.

If the canonical skill is unavailable, explain that this shortcut requires interview-coach and help locate/install it; do not claim its persistence or workflow has been loaded.
